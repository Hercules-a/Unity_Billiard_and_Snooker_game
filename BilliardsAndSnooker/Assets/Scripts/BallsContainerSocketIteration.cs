using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class BallsContainerSocketIteration : MonoBehaviour
{
    public int AvaibleSocket;
    // Start is called before the first frame update
    void Start()
    {
        AvaibleSocket = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void AvaibleSocketSetToZero()
    {
        AvaibleSocket = 0;
    }
}
